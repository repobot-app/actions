# action.yml
name: 'RuboCop'
description: 'Runs RuboCop'
inputs:
  path:
    description: 'Path to file(s)'
    required: false
    default: '**/*.rb'
outputs:
  result_file:
    description: 'Result file'
runs:
  using: 'docker'
  image: 'docker://solnic/ah-action-rubocop:latest'
  args:
    - ${{ inputs.path }}
