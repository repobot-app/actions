name: "RuboCop"
on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  diff:
    runs-on: ubuntu-latest
    name: Check diff
    outputs:
      files: ${{ steps.get_diff.outputs.diff }}
    steps:
    - name: Clone
      uses: actions/checkout@v2
    - name: Get git diff
      id: get_diff
      uses: technote-space/get-diff-action@v4
      with:
        PATTERNS: |
          **/*.rb
          **/*.rake
          Gemfile
          Rakefile

  run:
    runs-on: ubuntu-latest
    needs: diff
    if: needs.jobs.diff.outputs.files != ''
    name: ${{ matrix.type }}
    strategy:
      fail-fast: false
      matrix:
        type: ["Style", "Layout", "Naming", "Lint", "Metrics", "Security"]
    steps:
    - name: Clone
      uses: actions/checkout@v2
    - name: Check ${{ matrix.type }}
      uses: action-hero/actions/rubocop@main
      env:
        GIT_DIFF: ${{ needs.jobs.diff.outputs.files }}
      with:
        type: ${{ matrix.type }}
